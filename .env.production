# Domain
# Used by Traefik to transmit traffic and acquire TLS certificates
DOMAIN=speats.ch

# Traefik - Let's Encrypt
# Email per notifiche scadenza certificati SSL (obbligatoria)
EMAIL=bandigare@gmail.com

# Credenziali per accesso dashboard Traefik (https://traefik.speats.ch)
# Genera HASHED_PASSWORD con: echo $(htpasswd -nb admin TuaPassword) | sed -e s/\\$/\\$\\$/g
USERNAME=admin
HASHED_PASSWORD=$$apr1$$CAMBIA_QUESTO_HASH

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=https://dashboard.speats.ch

# Environment: local, staging, production
ENVIRONMENT=production

PROJECT_NAME="AI speats"
STACK_NAME=ai

# Backend
BACKEND_CORS_ORIGINS="https://speats.ch,https://dashboard.speats.ch,https://api.speats.ch"
SECRET_KEY=CAMBIA_QUESTA_CHIAVE_SEGRETA_IN_PRODUZIONE
FIRST_SUPERUSER=bandigare@gmail.com
FIRST_SUPERUSER_PASSWORD=CAMBIA_QUESTA_PASSWORD_IN_PRODUZIONE

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@speats.ch
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=speats-ai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CAMBIA_QUESTA_PASSWORD_IN_PRODUZIONE

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
